# Copy to .env — do not commit .env. See ../docs/chatbot-environment.md
MONGO_URI=your_mongodb_connection_string_here
PORT=3000

# Required for POST /api/rag/search (same key as the Expo app uses for Gemini)
GEMINI_API_KEY=